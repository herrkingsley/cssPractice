TODO
Header without using flex or grid.

1. header that uses absolute and is fixed = old browser support!
2. header that has a dropdown menu
3 header that has a background image and possibly uses sticky


background-image
box-model, padding,  border, margin
use rem, ems and % to make responsive
display values
position
